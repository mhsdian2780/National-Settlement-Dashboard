# 另一台电脑更新看板 · 复制粘贴话术

> 在另一台电脑的 WorkBuddy 里，**整段复制下面的内容**发出去即可。
> 前提：那台电脑的 WorkBuddy 已登录、且已关联腾讯文档「全国配套项目执行追踪群-H2」。

---

## 【话术一】首次使用（那台电脑从来没跑过）

```
帮我更新「全国配套项目 · 完成核销情况一览表」看板。

项目在 GitHub 仓库 https://github.com/mhsdian2780/National-Settlement-Dashboard
请按下面步骤做：

1. 先把仓库克隆到本地固定路径 C:/Users/<那台电脑的用户名>/WorkBuddy/ 下（如果已存在就
   先 git pull --rebase 拉最新的）。

2. 数据源是腾讯文档「全国配套项目执行追踪群-H2」，file_id 是 IPItsCBmNiMC。
   用这个表的最新数据依次跑三个脚本：
   python export_sheets.py    # 导出 11 张 sheet 到 raw/*.csv
   python build_data.py       # 解析生成 data.json
   python build_index.py      # 生成 index.html

3. 必须检查这两个结果，不对就别继续：
   - build_data.py 要打印「校验：按区域聚合 done=xxxx vs 按项目合计 done=xxxx -> ✅ 一致」
     如果是 ❌，说明区域映射错了（最常见是把「西部区域」和「西北区域」合并了，这两个是
     独立区域，绝对不能合并）。
   - build_index.py 要打印「战役=13」，且已开展场次应接近 2746。

4. 跑完后把结果推回 GitHub：
   git add -A && git commit -m "更新核销数据" && git push

5. 告诉我新的数字（规划/已开展/已核销）和线上是否已更新。

注意：
- 这台电脑如果没装 Python，直接用你自带的 Python 环境跑就行。
- 腾讯文档连接器如果提示要授权，先让我授权。
- git push 如果需要登录 GitHub，用 mhsdian2780 账号（邮箱 523969627@qq.com）。
```

---

## 【话术二】日常更新（那台电脑已经跑过一次，以后每周用）

```
更新核销看板：用腾讯文档「全国配套项目执行追踪群-H2」的最新数据
重跑 export_sheets.py → build_data.py → build_index.py，
确认校验 ✅ 一致、战役=13 之后，git add -A && git commit && git push。
然后把新的规划/已开展/已核销数字报给我。
```

---

## 【话术三】只想要一份新的 index.html，不想碰 git

```
用腾讯文档「全国配套项目执行追踪群-H2」（file_id IPItsCBmNiMC）的最新数据，
重新生成核销看板的 index.html 给我，不用管 git。
重点确认 build_data.py 的校验是 ✅ 一致、战役=13。
```

---

## 关键参数备查（话术里已含，此处仅备份）

| 项 | 值 |
|---|---|
| 仓库 | https://github.com/mhsdian2780/National-Settlement-Dashboard |
| 线上链接 | https://mhsdian2780.github.io/National-Settlement-Dashboard/ |
| 腾讯文档 | 「全国配套项目执行追踪群-H2」，file_id `IPItsCBmNiMC` |
| GitHub 账号 | `mhsdian2780` / `523969627@qq.com` |
| 校验基线 | 13 个项目 · 规划 3141 · 已开展 2746 · 已核销 207 |
| 区域铁律 | 西部区域 ≠ 西北区域，绝不能合并 |
