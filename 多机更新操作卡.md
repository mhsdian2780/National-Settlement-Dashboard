# 多机更新操作卡（方案 A：GitHub 仓库为唯一真相源）

> 打印这一页贴在两台电脑旁即可。出差在外用另一台电脑，照做就行。

---

## 一、一次性准备（每台电脑只做一次）

### 1. 装好三样东西

| 需要 | 说明 |
|------|------|
| **Git** | 官网下载安装即可，一路默认 |
| **Python 3.13** | 或用 WorkBuddy 自带环境（见下） |
| **WorkBuddy** | 已登录（腾讯文档连接器可用，用来拉数据） |

### 2. 克隆仓库

```bash
cd C:/Users/<你的用户名>/WorkBuddy
git clone https://github.com/mhsdian2780/National-Settlement-Dashboard.git
cd National-Settlement-Dashboard
```

放哪个路径都行，但**两台电脑建议保持一致**，方便你记。

### 3. 确认身份（只第一次需要）

```bash
git config --global user.name "mhsdian2780"
git config --global user.email "523969627@qq.com"
```

首次 `git push` 时，弹窗登录 GitHub 一次即可（推荐用浏览器授权）。

---

## 二、每周更新（三步 + 一推）

```bash
cd C:/Users/<你的用户名>/WorkBuddy/National-Settlement-Dashboard

# 0. 先拉最新（另一台电脑可能刚改过）
git pull --rebase

# 1. 拉腾讯文档最新数据
python export_sheets.py

# 2. 解析生成 data.json（会自校验）
python build_data.py

# 3. 生成新的 index.html
python build_index.py

# 4. 发布
git add -A
git commit -m "更新核销数据 YYYY-MM-DD"
git push
```

推完 GitHub Pages 自动发布，**访问链接永远不变**。

> **最省事**：直接跟 WorkBuddy 说「**更新数据**」，它会跑完 0–3 步，
> 然后你只需在本机执行第 4 步（或让它一起做）。

---

## 三、必须检查的两个信号

**`build_data.py` 输出：**
```
校验：按区域聚合 done=xxxx vs 按项目合计 done=xxxx -> ✅ 一致
```
出现 ❌ 就**别推送**，说明区域映射错了。

**`build_index.py` 输出：**
```
OK 生成 index.html (xx,xxx bytes) | 战役=13 规划=xxxx 已开展=xxxx 已核销=xxx
```
战役应该恒为 **13**，合计应等于 3141 / 2746 / 207 附近（随更新变动）。

---

## 四、两台电脑的规矩

1. **不要同时改。** 一台在改，另一台先别动。
2. **开工先 `git pull --rebase`**，收工再 `push`。这样永远不冲突。
3. **GitHub 是唯一真相源。** 资料库团队空间那份只是备份/分发，不用管它。
4. 如果 push 被拒（提示 `rejected`），说明另一台先推了：
   ```bash
   git pull --rebase
   git push
   ```

---

## 五、出问题时

| 现象 | 处理 |
|------|------|
| `git pull` 报冲突 | 跟 WorkBuddy 说「git 冲突了，帮我看看」 |
| push 要求登录 | 弹窗里选「用浏览器登录」 |
| Python 找不到 | 跟 WorkBuddy 说「更新数据」，用它的环境跑 |
| 校验 ❌ | 检查腾讯文档里区域名有没有改名 |
| 页面没变 | 等 1–2 分钟，Pages 构建有延迟 |
| 想回退 | `git log --oneline` 找到上一个 commit，`git revert <sha>` |

---

## 六、相关链接与路径

- **仓库**：https://github.com/mhsdian2780/National-Settlement-Dashboard
- **对外链接**：`https://mhsdian2780.github.io/National-Settlement-Dashboard/`
- **数据源**：腾讯文档「全国配套项目执行追踪群-H2」（file_id `IPItsCBmNiMC`）
- **备份副本**：资料库团队空间「全国配套核销看板」节点 `WeB5lEovudfa8O8NY4zUFd`
