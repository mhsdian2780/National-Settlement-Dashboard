# 全国配套核销看板 · 多机使用说明

这里是**完整的可更新项目**，不是只看不能改的成品。

**唯一真相源 = GitHub 仓库**：https://github.com/mhsdian2780/National-Settlement-Dashboard

在任何一台电脑上 `git clone` 下来，跑三步更新，`git push` 回去即可。
两台电脑（台式 + 笔记本）轮流更新，永远以仓库里的版本为准。

> 简明版操作步骤见同目录下的 **`多机更新操作卡.md`**（建议打印）。
> 本文是详细说明。

---

## 一、在新电脑上怎么开始（重要）

**新电脑只需一条命令**（前提：已装 Git）：

```bash
cd C:/Users/<你的用户名>/WorkBuddy
git clone https://github.com/mhsdian2780/National-Settlement-Dashboard.git
cd National-Settlement-Dashboard
```

所有源文件、模板、脚本、封面图、文档都会完整拉下来，目录结构与本机一致。
**不需要**再去资料库下载，也**不需要**手动拷文件。

### 如果确实拿不到仓库（例如网络受限）

资料库团队空间「全国配套核销看板」里存了同一份文件的托管副本，可以跟 WorkBuddy 说：

> 从资料库节点 `WeB5lEovudfa8O8NY4zUFd` 把文件下载到本地

拿到的是完整 21 个源文件。但请注意：**托管副本的 HTML 会被平台改写**
（注入 `pnid` 标记和 `inject.js`），且它不是版本受控的。
所以这只是应急路径，日常请走 `git clone`。

### 跑更新

```bash
cd "C:/Users/<你的用户名>/WorkBuddy/National-Settlement-Dashboard"

PY="C:/Users/<你的用户名>/.workbuddy/binaries/python/versions/3.13.12/python.exe"

$PY export_sheets.py    # 1. 拉取腾讯文档最新数据
$PY build_data.py       # 2. 解析生成 data.json（会自动校验）
$PY build_index.py      # 3. 生成新的 index.html

git add -A && git commit -m "更新核销数据" && git push   # 4. 发布
```

> **最省事的做法**：直接在新电脑上跟 WorkBuddy 说「**更新数据**」，
> 它会自动跑完 1–3 步，你只需确认后再推送。

### 关于「更新链接」

**GitHub Pages 的对外链接永远不变**：
`https://mhsdian2780.github.io/National-Settlement-Dashboard/`

只要 `git push`，1–2 分钟后线上自动更新，协作方**无需换链接**。

### 准备（每台电脑只做一次）

1. 装 Git
2. 登录 WorkBuddy（腾讯文档连接器可用，用来拉数据）
3. 首次 push 时按弹窗登录一次 GitHub

---

## 二、目录里都有什么

| 文件 | 作用 | 要不要动 |
|------|------|---------|
| `export_sheets.py` | 从腾讯文档拉取 11 张 sheet → `raw/*.csv` | 不用动 |
| `build_data.py` | 解析 CSV → `data.json`（含区域归一、校验） | 不用动 |
| `build_index.py` | 把 `data.json` 注入模板 → `index.html` | 不用动 |
| `template.html` | 页面模板（样式 + 交互逻辑都在这里） | 改样式/逻辑时才动 |
| `data.json` | 结构化数据 | 自动生成 |
| `index.html` | **最终发布产物**，单文件零依赖 | 自动生成 |
| `cover.png` | 微信分享封面图 | 不用动 |
| `raw/` | 导出的中间 CSV，方便离线重算 | 自动生成 |
| `README.md` | 完整技术文档（口径、坑位、排序规则） | 参考 |
| `多机更新操作卡.md` | 两机轮换更新的简明步骤（建议打印） | 参考 |
| `.github/workflows/pages.yml` | GitHub Pages 自动发布配置 | 不用动 |
| `.nojekyll` | 让 Pages 原样发布，不做 Jekyll 处理 | 不用动 |

---

## 三、更新流程详解

### 检查结果

第 2 步必须看到这行才算正常：

```
校验：按区域聚合 done=xxxx vs 按项目合计 done=xxxx -> ✅ 一致
```

如果出现 ❌，**先别发布**，说明区域映射出问题了（最常见是把「西部区域」和「西北区域」合并了，
这两个是独立区域，绝不能合并）。

第 3 步会输出：

```
OK 生成 index.html (xx,xxx bytes) | 战役=13 规划=xxxx 已开展=xxxx 已核销=xxx
```

### 发布

用浏览器打开 `index.html` 确认没问题后，把改动推到 GitHub 即可：

```bash
git add -A
git commit -m "更新核销数据"
git push
```

GitHub Pages 会自动发布，访问链接不变。

> **两台电脑轮换时的黄金顺序**：开工先 `git pull --rebase`，收工再 `git push`。
> 这样永远不会有冲突。

---

## 四、需要你注意的几点


**1. 数据源固定**
所有分配/完成/核销数据以**各项目明细 sheet** 为准，「项目总览」表只用来确认项目清单和分类。
（总览里的区域分配列已删除，分配数不准。）

**2. 13 个项目，两条线**

| 肺肿线 | 数量 | 项目 |
|--------|-----|------|
| 早肺 | 5 | 美乐肺辅-区域赛、美乐肺辅-省级赛、胸外老友线上会、肺凡新生-患教项目（线上）、云端ARTS2.0（停止） |
| 晚肺 | 8 | 靶化上市会（地区级线下）、靶化上市会-线上会、肺凡新生-线下患教项目、肺凡新生-大咖直播、53学苑-大咖线上会、53学苑-MDT、53学苑-大咖行、CACA基层行 |

**没有「未分类」这条线。**

**3. 云端ARTS2.0 是隐藏 sheet**
该项目虽已停止，但仍需统计，它的 sheet 在腾讯文档里是隐藏状态，导出脚本已显式包含。

**4. 排序规则**
所有展示区（按区域 / 按战役 / 明细表 / 弹层明细）**统一按核销率从高到低排序**，
每次更新后自动生效；核销率算不出的项（已开展为 0）自动置底。这条不用手工调。

**5. 多台电脑同步建议**
- **GitHub 仓库是唯一真相源**，两台电脑都 clone 它、都往它 push
- 开工先 `git pull --rebase`，收工再 `git push`
- 不要两台机器同时改同一份文件
- 资料库团队空间那份只是**备份/分发副本**，不用在它上面做更新

---

## 五、常见问题

**Q：一定要装 Python 吗？**
A：更新数据需要。或者直接用 WorkBuddy 说「更新数据」，它会用自带环境跑。

**Q：想让领导只看结果，不需要改？**
A：直接把 `index.html` 或者 GitHub Pages 链接发出去就行，单文件零依赖，手机也能打开。

**Q：改样式怎么做？**
A：改 `template.html`，然后重跑第 3 步 `build_index.py`。设计规范见 `README.md`。

**Q：数据校验报 ❌ 怎么办？**
A：检查腾讯文档里各项目 sheet 的区域名有没有新增或改名。区域映射表在 `build_data.py`
顶部的 `REGION_ALIAS`，必要时补充新叫法。

**Q：我在本地更新完，资料库里那个在线看板会自动变吗？**
A：**不会，也不需要它变。** 资料库节点是独立托管的另一份，我们把它定位为**备份/分发副本**。
日常对外发布走 GitHub Pages 链接，`git push` 之后线上自动更新，完全不用碰资料库。

**Q：两台电脑会不会冲突？**
A：只要遵守「开工先 `git pull --rebase`、收工再 `git push`、不同时改」这三条，就不会冲突。
万一 push 被拒，先 `git pull --rebase` 再 `git push` 即可。

**Q：从资料库下载下来的 `template.html` / `index.html` 和我本地的不完全一样，有问题吗？**
A：没问题，但**不要用托管副本覆盖仓库版本**。托管平台会在 HTML 里注入标记
（`pnid`、`data-page-node-id` 属性、`inject.js` 脚本引用）。三个 Python 脚本不会被改动。
正式更新请一律用 `git clone` / `git pull` 拿到干净版本。

**Q：哪份是准的？**
A：**GitHub 仓库为准**（`mhsdian2780/National-Settlement-Dashboard`，main 分支）。
本机 clone 出来的目录、GitHub Pages 线上页面，都源自它。
资料库团队空间那份是备份，仅供参考取源。

**Q：新电脑没有 git 仓库怎么办？**
A：`git clone` 一条命令就有了（见第一章）。如果只是临时要一份 `index.html` 发人，
不装 git 也行 —— 跑完三步直接打开本地的 `index.html` 即可，
只是这次改动不会同步到仓库和线上。

